# V1 Implementation Status

This file tracks current progress against `GOAL.md`. It is intentionally conservative: an item is marked complete only when implemented and covered by the current test suite or smoke checks.

## Implemented

- Cargo-built Rust `yara-x-parser` bridge exposed through a panic-safe C ABI.
- Versioned cbindgen-generated C++ ABI for parsed YARA rules (`re_yara_bridge_version() == 1`).
- C++ bridge conversion from Rust-owned parser arena views into the internal `ParsedRuleSet` and `Expression` model.
- Include resolver for root files, including-file directory lookup, explicit include directories, include cycle diagnostics, and CLI `-I`/`--include-dir` support.
- Source names and stable numeric source ids carried on parsed rule/expression spans so included-file semantic diagnostics and compact IR artifacts can point at the included source.
- C++ semantic validation for known module imports, module fields, and module functions, including function arity and statically known argument types.
- C++ semantic validation infers known `with`-local alias types over symbolic fact-backed expressions for downstream function argument checks.
- Semantic validation rejects unsupported YARA expression nodes and unknown module functions instead of letting them evaluate as false.
- Compiler-level source namespaces, same-namespace duplicate diagnostics, qualified cross-namespace rule references, dependency collection, fact propagation, and cycle diagnostics.
- Private rule suppression, global rule gating, and YARA-X-style validation that global rules depend only on global rules.
- Descriptor-backed global/external variables lowered from bare identifiers into async fact requirements.
- Descriptor-backed module function calls lowered into async fact requirements with typed argument-derived provider keys.
- Extended string operators `icontains`, `startswith`, `istartswith`, `endswith`, `iendswith`, and `iequals` bridged from YARA-X and evaluated by the VM.
- Integer arithmetic and bitwise expression nodes bridged from YARA-X and evaluated by the VM.
- Built-in integer readers (`uint8`, `int8`, `uint16`, `uint16be`, `int16`, `uint32`, `uint32be`, etc.) bind to `process.image.bytes` and are evaluated by the VM from provider-supplied bytes.
- Pattern count/offset/length constructs (`#`, `@`, `!`) bridged from YARA-X and evaluated from provider-supplied `PatternValue` metadata.
- Pattern-set `of` expressions for `all`, `any`, `none`, numeric, and percentage quantifiers over rule pattern facts, including `at` and `in` anchor filters over provider-supplied match offsets.
- Boolean tuple `of` expressions for `all`, `any`, `none`, numeric, and percentage quantifiers over condition tuples.
- Numeric zero pattern quantifiers use YARA's clarified exactly-zero semantics for both `of` and `for..of` pattern sets.
- `with` expressions with local symbolic bindings for aliases over fact-backed expressions.
- `for in` expressions over integer ranges, expression tuples, arrays, and objects with value and key/index-value local bindings.
- `for of` pattern-set loops with current-pattern placeholder binding for `$`, `#`, `@`, and `!` body expressions plus `all`, `any`, `none`, numeric, and percentage quantifiers.
- YARA-X-style undefined propagation for boolean contexts: `and`/`or` treat undefined operands as false, while `not`, comparisons, and string predicate operators return undefined when an operand is undefined.
- Array and object `Value` storage plus lookup expressions for integer indexes and string keys.
- Synchronous VM step model that pauses on missing facts and resumes after `FactCache` fill.
- VM evaluation of rule references with per-step memoization.
- Descriptor-backed fact routing for process, PE, and fixture pattern facts.
- `FactCache` uses an indexed subject-id/fact-key map for hot-path lookup while preserving deterministic subject snapshots and TTL expiration behavior.
- TTL-based session cache expiration for volatile facts.
- Cache policy tests cover volatile process facts with `0s` TTL separately from static PE image facts, including richer `pe.sections` and `pe.imports` array facts with `30s` TTL.
- Staged scheduler prefetches cheap descriptor facts first and defers expensive scan facts until symbolic evaluation reaches them, allowing cheap process/PE filters to short-circuit before scan requests.
- Cheap descriptor-backed module function calls with statically known arguments lower into deterministic provider fact keys and participate in staged prefetch before expensive facts.
- Versioned deterministic binary IR and schedule artifacts alongside readable dumps, including source tables and per-rule source ids.
- Debug IR dumps/artifacts use schema `rule-engine-debug-ir.v1`; schedule dumps/artifacts use schema `rule-engine-schedule.v1`.
- IR and schedule dumps/artifacts include rule namespace names and qualified rule identifiers.
- Versioned `RETR` evaluation trace artifacts use schema `rule-engine-evaluation-trace.v1`, capture subject, provider facts, final VM step, opt-in expression trace events, and replay deterministically without TCP or live providers.
- Trace capture snapshots all cached facts for the traced subject, including runtime-derived custom module function facts, so function-call decisions replay without provider access.
- Length-prefixed protocol frame codec.
- Typed v1 protocol payload codecs for handshake capabilities, subject lists, batched fact requests, and fact responses, including pattern match metadata values.
- Windows process subject enumeration.
- Windows process snapshot fact provider for PID, parent PID, thread count, architecture, integrity level, user SID, user name, token elevation, token type, loaded module count/names, memory region counts/region metadata arrays, process name, image path, session id, and command line, with structured diagnostics for unsupported or inaccessible fields.
- Windows process handle fact provider for `process.handles.count` on `endpoint.process.handles`, including access-denied and partial-data diagnostics.
- Windows Authenticode signer fact provider for `process.signer.status` on `endpoint.process.signer`, returning stable status strings from WinTrust results.
- Windows process providers have regression coverage for exited-process lifetime races returning unavailable facts with diagnostics.
- Windows PE image fact provider for validity, machine, section count, entry point, image size, subsystem, file and DLL characteristics, COFF timestamp, `pe.sections` section object arrays, `pe.imports` import object arrays, `pe.exports` export object arrays, `pe.debug_entries` debug directory object arrays, `pe.resources` resource data object arrays, `pe.certificates` certificate-table object arrays, and `pe.tls_callbacks` TLS callback object arrays.
- Windows PE parser regression coverage includes synthetic non-PE, malformed optional-header, truncated optional-header, PE32, and PE32+ fixture images.
- Fixture-backed pattern fact provider for `$name.matches` and `$name.pattern` facts with scan-space and match-context metadata.
- Configurable pattern fixture text input through `rule_engine_client --pattern-fixture <file>`.
- Configurable literal byte scanner in pattern fixture files through `scan` directives, returning real offsets, bytes, before/after context, scan-space names, and region permissions as `PatternValue` metadata.
- File-backed literal byte scanner in pattern fixture files through `scan_file` directives, resolving relative file paths from the fixture file and returning the same `PatternValue` match metadata.
- Rule-derived literal scan plans for text string patterns are attached to pattern fact requests and round-trip through the localhost protocol; `scan_file_space` fixture directives provide file-backed scan spaces without duplicating pattern definitions in client config.
- When no explicit pattern scan spaces are configured, the default Windows client adds subject-scoped `process.image.bytes` scan spaces for pattern requests by reading each requested process image file, with localhost integration coverage for matching the current process PE header.
- The scan route can return named scan spaces such as `process.image.bytes` as raw `bytes` facts for VM-owned built-in reader evaluation.
- Pattern fixture files can enable explicit mapped PE section scan spaces with `scan_process_image_sections`; the Windows client returns matches from subject-scoped `process.image.section.<name>` spaces with section-derived permissions.
- Pattern fixture files can enable readable memory-region scan spaces with `scan_readable_memory_regions`, optionally scoped by base address and size; the Windows client returns matching chunks from subject-scoped `process.memory.region.<address>` spaces with protection-derived permissions.
- One-shot localhost Asio client session for handshake capability advertisement, subject enumeration, batched fact requests, and fact responses.
- Localhost clients can bind custom fact-batch handlers for descriptor-backed module function routes while keeping clients limited to typed fact responses.
- Localhost clients can advertise custom provider route capabilities; evaluator orchestration rejects requests for routes missing from the client handshake.
- Verified programs expose a deduplicated provider-route registry from concrete facts and descriptor-backed function calls; client-backed evaluation preflights that registry against handshake capabilities before subject evaluation starts.
- Client-backed evaluator orchestration validates available provider fact values against descriptor-derived expected types before storing them in the session cache.
- The VM revalidates descriptor-backed cached field/global/pattern facts and function return facts before evaluation consumes them, producing no-match diagnostics for wrong-typed available cache entries.
- Provider function fact keys use a stable v1 argument-token encoder for booleans, integers, floats, strings, bytes, arrays, objects, pattern values, and explicit undefined values.
- `rule_engine_server --module-config <file>` loads custom module field/function/global descriptors before verification; `rule_engine_client --custom-fact-fixture <file>` advertises custom routes and serves configured typed fixture facts.
- `examples/custom_binding/` provides a tested custom descriptor, rule, and client fact fixture covering a custom field, module function binding, provider-backed global, and custom route capabilities.
- `examples/rule_corpus/` provides tested supported and intentionally unsupported YARA examples for the current parser and semantic subset.
- `docs/RUST_BRIDGE_UPDATE.md` documents the Rust YARA-X bridge update workflow, cbindgen header regeneration path, ABI rules, and required Rust/C++ verification commands.
- Field, function, and global descriptors carry provider request timeouts into lowered fact requirements; request batches grouped by route use the maximum descriptor timeout in the batch.
- Field, function, and global descriptors can opt into bounded retries for timed-out provider fact diagnostics and can carry descriptor-specific cancellation diagnostics; client-backed evaluation retries those facts before caching the diagnostic and synthesizes unavailable cancellation facts locally when stopped before provider dispatch. Custom module config files can express the same policy metadata.
- The draft binary IR and schedule artifacts are version 4 and include descriptor retry policy, retry budget, cancellation diagnostic metadata, and explicit schema identifiers alongside route, TTL, timeout, and cheap-prefetch scheduling fields.
- Client protocol sockets apply bounded read/write timeouts, and fact batch round-trips apply the request timeout advertised in the batch message.
- `rule_engine_client` serves localhost v1 client sessions with a default one-shot mode, bounded `--max-sessions <n>` mode, unbounded `--serve` mode, and opt-in bounded concurrent session workers through `--session-workers <n>`; `rule_engine_server` connects and performs process/PE fact round trips.
- Localhost client serving accepts an opt-in stop token for graceful listener shutdown, returns unavailable cancellation diagnostics instead of dispatching new provider work after shutdown, exposes that token to context-aware custom provider handlers for cooperative in-flight cancellation, and wires `rule_engine_client` console interrupts into the same stop path.
- `docs/TRANSPORT_BOUNDARY.md` documents that the plain TCP protocol is a localhost V1 demo path and lists the authentication, encryption, authorization, retry, cancellation, and payload-boundary work required before remote clients.
- `rule_engine_client` and `rule_engine_server` expose `--io-timeout-ms <ms>` for localhost protocol I/O deadlines.
- Client-backed VM orchestration can evaluate one subject or subject chunks over one connection, batching missing fact requests by provider route across subjects.
- Integration coverage evaluates multiple requested subjects over one localhost client connection with a custom provider route advertised during handshake and requested through the normal capability checks.
- `rule_engine_server --rule <rule.yar>` parses, verifies, evaluates the current process subject or `--all-subjects` with configurable `--subject-concurrency`, and prints per-subject rule results.
- `rule_engine_server --json` emits structured JSON for rule evaluation sessions and client smoke fact sessions, including handshake metadata, capabilities, evaluated subjects, rule results, diagnostics, fact statuses, typed fact values, and TTLs.
- `rule_engine_benchmark` generates initial synthetic baseline benchmark reports for shared process-name predicate workloads, including selective, broad nonselective, and mixed exact-VM-only plus expensive-leaf scenarios, with reproducible metadata including build type and enabled optimizer flags, JSON/Markdown output, provider elapsed counters, evaluator expression counters, canonical/exact-VM-only optimizer counters, optimizer predicate order/cost/selectivity observations, and fact-cache lookup/hit/miss/probe counters.
- Client-backed multi-subject evaluation can optionally record provider rounds, provider request messages, requested fact keys, returned facts, and provider round-trip elapsed time without letting clients evaluate predicates; the opt-in optimizer-plan client path also records candidate-provider request messages, requested filters, returned subject ids, broad results that cover every evaluated subject, unadvertised filters, and candidate-provider elapsed time.
- An offline optimizer POC extracts canonical descriptor-backed field/global comparison predicates, deduplicates owning rules, and reports exact-VM-only expression shapes without changing evaluation decisions.
- `build_optimizer_plan` creates the first opt-in server-owned optimizer artifact with canonical predicate nodes, static predicate order, exact-VM fallback notes, provider requirements, and generic candidate-provider requests; its JSON keeps candidate-provider request entries rule-free while retaining internal owner metadata for C++ diagnostics.
- Descriptor-owned fact cost classes now annotate built-in inventory, cheap process snapshot, static image header, process array, broad image array, handle/signer, memory-region, and pattern-scan facts; the offline optimizer carries those classes into canonical predicate reports.
- The optimizer POC can simulate a shared predicate DAG offline, reporting per-node matched/pruned/unknown subject sets, per-rule exact-VM candidate sets, and prune reasons while keeping missing, diagnostic, and unsafe boolean-context predicates in the exact-VM candidate path.
- Shared predicate DAG simulation now uses static descriptor cost ordering and reports predicate order plus observed selectivity ppm without changing final exact-VM evaluation.
- An opt-in prefiltered evaluation comparison now uses shared-DAG candidate sets to run exact VM only for surviving rule/subject pairs, synthesizes pruned no-match results, emits exact-VM skip trace events for pruned rule/subject pairs, and reports baseline parity mismatches while preserving diagnostic/unknown predicate subjects and expensive-first exact-VM-only diagnostics on the exact-VM path.
- `evaluate_with_optimizer_plan` adds the first plan-driven opt-in C++ sweep path: it consumes `OptimizerPlan`, builds shared-DAG candidate state, runs the exact VM only for surviving reportable rule/subject pairs, synthesizes no-match results for pruned pairs, emits exact-VM skip trace events, and keeps unavailable predicate facts on the exact-VM diagnostic path.
- `evaluate_with_optimizer_plan` can also consume plan-owned generic candidate-provider results before exact-VM final evaluation, reporting candidate-provider requests, returned subjects, broad results that cover every evaluated subject, and server-side fallback predicate evaluations while keeping provider-scope output separate from final rule decisions.
- `rule_engine_benchmark --simulate-optimizer-plan-prefilter` benchmarks the plan-driven opt-in sweep path directly; `production_scale_validation` currently reports 52 plan-driven exact-VM rule executions, 108 avoided exact-VM rule executions, 108 exact-VM skip trace events, zero result mismatches, and zero incomplete subjects in `build/codex-debug/reports/optimizer_plan_prefilter.{json,md}`.
- `rule_engine_benchmark --scenario global_gate_prefilter --simulate-optimizer-plan-prefilter` validates global-rule gating on the plan-driven path while keeping exact-VM execution and parity counters scoped to reportable rules; the unit report records six optimized reportable exact-VM executions, two avoided executions, zero result mismatches, and zero incomplete subjects in `build/codex-debug/reports/global_gate_prefilter.{json,md}`.
- Shared predicate DAG simulation classifies per-rule candidate sets as dense bitsets or sparse compact subject ids and reports approximate candidate-set bytes plus peak active-state bytes.
- Shared predicate DAG simulation can safely union simple top-level `or` alternatives made only of lifted descriptor predicates, pruning subjects that match none while keeping unknown/diagnostic subjects on the exact-VM path.
- Shared predicate DAG simulation marks rule branches with empty candidate sets as dropped, reports dropped-branch counts, and the lazy provider expansion POC skips provider batches for those branches.
- Shared predicate DAG simulation marks fully broad predicates as nonselective, keeps matched-count and selectivity metrics, and elides retained matched-subject ids so broad intermediate sets do not dominate optimizer state.
- Shared predicate DAG simulation emits optimizer trace events for static predicate ordering, rule-subject pruning, and nonselective broad predicates, including source spans and candidate-set size data.
- `rule_engine_benchmark --simulate-shared-predicate-dag` keeps baseline exact-VM execution enabled while adding simulated shared-predicate prune, optimizer trace-event, peak candidate-set subject, and peak candidate-set byte metrics to JSON/Markdown reports.
- `rule_engine_benchmark --scenario selectivity_feedback_inventory --simulate-selectivity-feedback` builds an observed-selectivity profile from a static warm-up shared-DAG sweep, uses it only as a same-cost predicate-order tie-breaker, and reports profile/reorder counters in `build/codex-debug/reports/selectivity_feedback.{json,md}`.
- Opt-in optimizer benchmark reports now carry bounded optimizer trace records in JSON and Markdown, including event, predicate id, rule, subject, reason, cost class, source span, and candidate-set counters for developer/operator debugging.
- The optimizer POC can derive source-linked discovery gates for canonical predicates shared by every reportable rule, simulate pack-level rejection before per-rule candidate expansion, and refuse to skip packs with unknown gate inputs.
- `rule_engine_benchmark --simulate-discovery-gates` adds discovery-gate count, evaluation, pack-skip, and trace-event metrics to JSON/Markdown reports, including the `discovery_gate_empty_pack` synthetic workload.
- `rule_engine_benchmark --simulate-prefiltered-evaluation` adds prefiltered exact-VM execution, avoided execution, exact-VM skip trace-event, result-mismatch, and incomplete-subject counters to JSON/Markdown reports, including the `or_process_name` synthetic set-operation workload.
- `rule_engine_benchmark --scenario production_scale_validation` adds a scalable unit-tier fixture shape with selective shared predicates, broad nonselective filters, expensive PE-import leaves, exact-VM-only `with` leaves, 10% controlled selectivity, and acceptance target metadata for 1-16 clients, 1,000 subjects/client, and 10,000 rules/pack.
- The optimizer POC can derive an offline lazy provider expansion plan from shared-DAG candidate sets, batching non-cheap required facts by route/key/type and preserving unknown subjects for exact-VM follow-up.
- `rule_engine_benchmark --simulate-lazy-provider-expansion` adds lazy provider batch/request/avoidance counters, including avoided expensive facts, for synthetic shared-name, empty-branch, and expensive PE-import workloads.
- The optimizer POC can derive generic candidate-provider requests such as `process.inventory.by_image_name` from canonical predicates, simulate provider-returned subject sets, and fall back to server-side per-subject predicate evaluation when the provider is unavailable.
- Candidate-provider request reports expose generic route/filter/argument data without owning rule identifiers, while the server-owned simulation still combines returned subject sets with the predicate DAG and exact-VM candidate path.
- `rule_engine_benchmark --simulate-candidate-provider` adds candidate-provider request, returned-subject, broad-result, and fallback-predicate counters to JSON/Markdown reports, including the `candidate_provider_unavailable` fallback scenario; unavailable provider results also emit optimizer trace records with the generic filter key and provider diagnostic before server-side predicate fallback.
- `rule_engine_benchmark --simulate-candidate-provider --simulate-optimizer-plan-prefilter --simulate-lazy-provider-expansion` now covers the combined plan-driven path; `shared_process_name_expensive_pe` reports one candidate-provider request, two returned candidate subjects, six avoided exact-VM rule executions, two avoided expensive `pe.imports` facts, zero result mismatches, and zero incomplete subjects in `build/codex-debug/reports/optimizer_plan_candidate_provider.{json,md}`.
- The v1 protocol now has generic candidate-provider capability metadata plus route/filter/argument request and status-bearing subject-set response codecs with diagnostics and TTLs; optimizer bridge coverage consumes those protocol responses as optional candidate-provider inputs while preserving exact-VM final evaluation.
- The localhost client session helpers can round-trip generic candidate-provider requests and responses through an optional extra handler while keeping fact-batch and candidate-provider response validation separate.
- An opt-in localhost optimizer-plan evaluator can request advertised candidate-provider filters before exact VM, feed returned subject sets into the server-owned optimizer plan, then materialize provider facts only for surviving exact-VM rule/subject pairs; if the filter is broad, exact VM still runs for every surviving rule/subject pair and the broad-result counter records the nonselective provider output; if the filter is not advertised, it skips the candidate-provider frame and rebuilds candidates from ordinary fact batches. The session result exposes evaluated subjects, the fact snapshot, and candidate-provider results, and `replay_optimized_client_evaluation` reconstructs the same optimized sweep without live providers; the parity-report helper also counts replay drift across subjects, rule results, optimizer traces, and sweep metrics.
- Optimizer-plan benchmark JSON/Markdown reports now include replay parity counters for subject, rule-result, optimizer-trace, and metric drift, using the same synthetic fact snapshot and candidate-provider results that shaped the captured optimized sweep.
- `rule_engine_benchmark --simulate-watchdogs` adds trace-only watchdog budget evaluation, budget-event, predicate-event, and route-event counters to JSON/Markdown reports; the first POC classifies broad shared predicates and oversized lazy provider route batches without enforcing cooldowns or changing rule results.
- `rule_engine_benchmark --simulate-watchdog-enforcement` adds opt-in explicit budget diagnostic counters while preserving trace-only watchdog mode as the default; `production_scale_validation` reports one deferred broad-predicate diagnostic and `mixed_process_name_expensive_pe` reports one timeout diagnostic for the oversized `pe.imports` route batch in `build/codex-debug/reports/watchdog_enforcement_predicate.{json,md}` and `build/codex-debug/reports/watchdog_enforcement_route.{json,md}`.
- `rule_engine_benchmark --simulate-static-fact-cache` adds offline content-addressed static image fact cache lookup/hit/miss/reuse/invalidation/subject-scoped counters to JSON/Markdown reports; the POC emits accepted/rejected reuse trace events and keeps volatile process inventory facts subject-scoped.
- A server-owned `StaticFactCache` primitive can reuse content-addressed static facts across subjects when route/key and verified file identity match, rewrites reused facts to the requesting subject id, emits accepted/rejected reuse trace events, invalidates changed identities, and refuses volatile process inventory facts.
- The optimizer can derive static fact cache candidates from server-owned identity facts in `FactCache`, requiring complete available path/file-id/size/last-write observations and carrying optional hash, signature/catalog, and scan-space identity into static cache keys while skipping volatile provider requirements.
- The opt-in localhost optimizer-plan evaluator can use caller-supplied static fact cache candidates, derive them from a supplied server-owned identity fact snapshot, or prefetch identity facts from a configured provider route during provider materialization, avoiding repeated static provider fact requests across subjects with identical verified file identity while preserving final exact-VM results and replay-compatible static-cache trace events.
- The Windows PE provider and default module registry now expose production cache-identity facts (`pe.identity.path`, `pe.identity.file_id`, `pe.identity.file_size`, `pe.identity.last_write_time`, `pe.identity.scan_space_name`, `pe.identity.scan_space_version`) on `endpoint.process.image.pe`; identity-only route requests use Win32 metadata without requiring a full PE parse, and optimizer-plan prefetch defaults to those keys.
- Runtime evaluation instrumentation and server JSON/text output now include `staticFactCacheProviderFactKeysAvoided`; live localhost optimizer-plan regressions prove production PE identity prefetch can reuse PE header facts plus `pe.imports`, `pe.exports`, `pe.resources`, `pe.debug_entries`, `pe.certificates`, and `pe.tls_callbacks` array facts across two distinct processes mapping the same image while avoiding the second provider fact key.
- The v1 fact-batch codec now carries bytes, bounded arrays, and bounded objects, including nested rich values, and rejects oversized structured-value counts before reading entries; provider fixture tests still cover non-empty export/resource/certificate/TLS object payload shapes when common live executable images expose empty arrays for those directories.
- `rule_engine_benchmark --simulate-scheduler-controls` adds stress-tier offline scheduler control counters for 10,000 jittered simulated clients, wake smoothing, VM/provider queue peaks, backpressure events, deadline misses, and compact idle-state bytes without changing runtime server scheduling; the simulation now uses the production evaluation-scheduler jitter and idle-state primitive.
- The production scheduler primitive can derive deterministic per-client timer jitter, compact idle state, due-client admission counts, active-sweep limits, deferred-client counts, and deadline-miss metrics without moving rule semantics or match decisions to clients.
- Localhost client evaluation now exposes opt-in runtime queue pressure counters for ordinary and optimizer-plan sessions, including peak pending VM subjects, peak pending provider requests, and threshold-crossing backpressure events while preserving exact VM final decisions.
- `rule_engine_server --json` now includes runtime evaluation instrumentation for VM/provider queue peaks, provider request work, elapsed provider time, candidate-provider counters, and backpressure events; CLI threshold knobs can classify VM subject and provider request pressure without changing rule results.
- Localhost provider serving can now separate accepted sessions into a bounded worker pool when `max_session_workers` / `--session-workers` is set, preserving the serial default while preventing an unbounded thread-per-session model.
- `rule_engine_benchmark --scenario production_scale_validation --simulate-optimization-comparison` emits the current comparison report shape, including 108 avoided exact-VM rule executions, 18 avoided expensive provider fact opportunities, 560 avoided expression evaluations, zero result mismatches, zero incomplete subjects, bounded broad candidate state, and zero optimizer-plan replay drift across subjects, rule results, optimizer traces, and sweep metrics for the unit-tier fixture.
- Custom Catch2 test main disables Windows CRT abort/report dialogs so failing tests terminate in console/CTest output instead of blocking on modal UI; a child abort probe regression test verifies unattended aborts do not hang behind a modal dialog.
- CLI targets: `rule_engine_check`, `rule_engine_server`, `rule_engine_client`, and `rule_engine_benchmark`.

## Remaining Full-V1 Work

- No currently tracked V1 gaps in this status file.
